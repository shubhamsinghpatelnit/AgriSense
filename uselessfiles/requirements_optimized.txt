# Memory-optimized requirements for disease detection API
# Designed to minimize RAM usage while maintaining functionality

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Image processing (lightweight versions)
Pillow==10.1.0
opencv-python-headless==4.8.1.78  # Headless version uses less memory

# Machine Learning (CPU-optimized)
torch==2.1.0+cpu -f https://download.pytorch.org/whl/torch_stable.html
torchvision==0.16.0+cpu -f https://download.pytorch.org/whl/torch_stable.html

# Basic ML utilities
numpy==1.24.3
scikit-learn==1.3.0

# System monitoring
psutil==5.9.6

# Optional: For explanations (install only if needed)
# grad-cam==1.4.8

# Development and testing
pytest==7.4.3
requests==2.31.0

# Note: Removed heavy dependencies like:
# - matplotlib (use only for development)
# - seaborn (not needed for API)
# - jupyter (not needed for API)
# - pandas (only if absolutely necessary)
# - tensorflow (not needed with PyTorch)